name: Integration Tests

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
    paths:
      - 'apps/cli/**'
      - 'libs/**'
      - 'apps/cli-e2e/**'
      - 'apps/cli-wterm-host/**'
      - 'apps/desktop/**'
      - 'apps/desktop-e2e/**'
      - '.github/workflows/integration.yml'
  workflow_dispatch:

concurrency:
  group: integration-tests
  cancel-in-progress: false # test repo is shared state — queue, don't cancel

permissions:
  actions: read
  contents: read

jobs:
  integration:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - run: npm ci

      - name: Install Playwright Chromium
        run: npx playwright install --with-deps chromium

      - name: Authenticate gh CLI
        run: echo "${{ secrets.INTEGRATION_TEST_PAT }}" | gh auth login --with-token

      # desktop-e2e drives the real Electron app, which needs a display.
      - name: Install xvfb
        run: sudo apt-get update && sudo apt-get install -y xvfb

      - name: Build CLI
        run: npx nx build cli

      - name: Run e2e tests (including integration)
        run: npx nx e2e:integration cli-e2e
        env:
          GH_TOKEN: ${{ secrets.INTEGRATION_TEST_PAT }}
          TEST_REPO: ${{ vars.TEST_REPO || 'kirby-test-runner/kirby-integration-test-repository' }}
          # Wire Kirby's logger to the file the "Upload debug logs"
          # step below already publishes. cli-wterm-host forwards
          # process.env into every spawned Kirby PTY
          # (apps/cli-wterm-host/src/main.ts:70), so this lands in
          # every test's Kirby instance with no fixture changes.
          KIRBY_LOG: /tmp/kirby-integ-debug.log
          KIRBY_LOG_LEVEL: debug

      - name: Run desktop e2e tests (including integration)
        run: npx nx e2e:integration desktop-e2e
        env:
          GH_TOKEN: ${{ secrets.INTEGRATION_TEST_PAT }}
          TEST_REPO: ${{ vars.TEST_REPO || 'kirby-test-runner/kirby-integration-test-repository' }}

      - name: Upload debug logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: kirby-debug-logs
          path: /tmp/kirby-integ-debug.log
          if-no-files-found: ignore
          retention-days: 7

      - name: Upload Playwright test output on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-test-output-integration
          path: |
            apps/cli-e2e/test-output
            apps/desktop-e2e/test-output
          if-no-files-found: ignore
          retention-days: 7
